language: java
before_install:
  - "export DISPLAY=:99.0"
  - "sh -e /etc/init.d/xvfb start"
  - "sudo apt-get install rubygems"
  - "sudo gem install compass"
  - "npm --silent install bower grunt-cli phantomjs karma"
install: mvn --quiet install integration-test
jdk:
  - oraclejdk7
notifications:
  email:
      - cmordant1@gmail.com
